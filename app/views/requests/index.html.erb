<h1>All requests</h1>
<% @requests.each do |request| %>
  <%= link_to request.name, request_path(request) %>
  <%= request.email %>
  <%= request.department %>
  <%= request.message %>
  <hr>
<% end %>
